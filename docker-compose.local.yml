services:
  idp:
    build:
      context: ./idp
      dockerfile: Dockerfile
    container_name: 018-hai-idp
    hostname: idp
    restart: unless-stopped
    depends_on:
      postgres-idp:
        condition: service_healthy
      redis:
        condition: service_healthy
      kafka:
        condition: service_healthy
    environment:
      BLOCKING_TIME_EXPONENTIATION_BASIS: ${BLOCKING_TIME_EXPONENTIATION_BASIS}
      MAX_LOGIN_ATTEMPTS_BEFORE_BLOCK: ${MAX_LOGIN_ATTEMPTS_BEFORE_BLOCK}
      MIN_TIME_BETWEEN_ATTEMPTS_IN_SECONDS: ${MIN_TIME_BETWEEN_ATTEMPTS_IN_SECONDS}
      EXPIRATION_TIME_RESET_TOKEN_IN_HOURS: ${EXPIRATION_TIME_RESET_TOKEN_IN_HOURS}
      ACCESS_TOKEN_DURATION_MINUTES: ${ACCESS_TOKEN_DURATION_MINUTES}
      REFRESH_TOKEN_DURATION_DAYS: ${REFRESH_TOKEN_DURATION_DAYS}
      PASSWORD_RESET_TOPIC: ${PASSWORD_RESET_TOPIC}
      ACCOUNT_BLOCKED_TOPIC: ${ACCOUNT_BLOCKED_TOPIC}
      ACCOUNT_CREATED_TOPIC: ${ACCOUNT_CREATED_TOPIC}
      JWT_SECRET: ${JWT_SECRET}
      REDIS_ADDR: ${REDIS_ADDR}
      LOGGER_TOPIC: ${LOGGER_TOPIC}
      MAIL_TOPIC: ${MAIL_TOPIC}
      KAFKA_CLIENT_ID: ${KAFKA_CLIENT_ID}
      BROKERS_ADDR: ${BROKERS_ADDR}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${IDP_DB_NAME}
      DB_HOST: ${IDP_DB_HOST}
      DB_PORT: ${DB_PORT}
      WEB_SERVER_PORT: ${WEB_SERVER_PORT}
      BASE_URL: ${BASE_URL}
      FIRST_RUN_ADMIN_EMAIL: ${FIRST_RUN_ADMIN_EMAIL}
      FIRST_RUN_ADMIN_PASSWORD: ${FIRST_RUN_ADMIN_PASSWORD}
      IDP_COOKIE_SECURE: ${IDP_COOKIE_SECURE}
      LOCAL_LOGIN_BYPASS_ENABLED: ${LOCAL_LOGIN_BYPASS_ENABLED:-false}
      # The IDP validates this bind at startup whenever local preview is enabled.
      # Missing, wildcard, LAN, and public binds fail closed before a session can
      # be issued; request Host headers are not part of the trust decision.
      GATEWAY_HOST_BIND: ${GATEWAY_HOST_BIND:-127.0.0.1}
      # "Sign in with Google": the client id/secret are shared with the backend's
      # Gmail connector, but the login redirect is a distinct path routed to the
      # IDP, so it needs its own registered redirect URI on the Google app.
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      GOOGLE_LOGIN_REDIRECT_URL: ${GOOGLE_LOGIN_REDIRECT_URL:-}
      # Password-recovery mail is unavailable until these are configured.
      # Use STARTTLS (normally port 587); do not expose this account publicly.
      SMTP_HOST: ${SMTP_HOST:-}
      SMTP_PORT: ${SMTP_PORT:-}
      SMTP_USERNAME: ${SMTP_USERNAME:-}
      SMTP_PASSWORD: ${SMTP_PASSWORD:-}
      SMTP_FROM: ${SMTP_FROM:-}
      SMTP_REQUIRE_STARTTLS: ${SMTP_REQUIRE_STARTTLS:-true}
    healthcheck:
      test: ["CMD-SHELL", "bash -ec '</dev/tcp/127.0.0.1/${WEB_SERVER_PORT}'"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    networks:
      - service-hub
      - idp-network

  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: 018-hai-backend
    hostname: backend
    restart: unless-stopped
    depends_on:
      postgres-automation:
        condition: service_healthy
      kafka:
        condition: service_healthy
      idp:
        condition: service_started
    environment:
      SERVER_PORT: ${SERVER_PORT}
      BASE_URL: ${BASE_URL}
      JWT_SECRET: ${JWT_SECRET}
      BACKEND_API_SHARED_KEY: ${BACKEND_API_SHARED_KEY}
      HAI_MEMORY_ENCRYPTION_KEY: ${HAI_MEMORY_ENCRYPTION_KEY}
      HAI_APPROVAL_PROOF_SIGNING_KEY: ${HAI_APPROVAL_PROOF_SIGNING_KEY}
      DB_USER: ${DB_USER}
      DB_PASSWORD: ${DB_PASSWORD}
      DB_NAME: ${AUTOMATION_DB_NAME}
      DB_HOST: ${AUTOMATION_DB_HOST}
      DB_PORT: ${DB_PORT}
      IMAGE_MAX_SIZE_IN_MB: ${IMAGE_MAX_SIZE_IN_MB}
      IMAGE_EXTENSIONS: ${IMAGE_EXTENSIONS}
      IMAGE_SAVE_DIR: ${IMAGE_SAVE_DIR}
      KAFKA_BROKERS: ${KAFKA_BROKERS}
      KAFKA_TOPIC: ${KAFKA_TOPIC}
      REDIS_ADDR: ${REDIS_ADDR}
      # Google OAuth (Gmail, Drive, Contacts, and Calendar). Empty by default;
      # when unset all four report unavailable instead of simulating access.
      GOOGLE_OAUTH_CLIENT_ID: ${GOOGLE_OAUTH_CLIENT_ID:-}
      GOOGLE_OAUTH_CLIENT_SECRET: ${GOOGLE_OAUTH_CLIENT_SECRET:-}
      GOOGLE_OAUTH_REDIRECT_URL: ${GOOGLE_OAUTH_REDIRECT_URL:-}
      HAI_OAUTH_TOKEN_ENCRYPTION_KEY: ${HAI_OAUTH_TOKEN_ENCRYPTION_KEY:-}
      HAI_OAUTH_STATE_SIGNING_KEY: ${HAI_OAUTH_STATE_SIGNING_KEY:-}
      # Per-client-IP request cap per minute; 0 disables it. Enforced through
      # Redis (REDIS_ADDR) so the count survives restarts and is shared across
      # instances. Was previously not passed through, so the limiter could never
      # be enabled regardless of .env.
      RATE_LIMIT_PER_MINUTE: ${RATE_LIMIT_PER_MINUTE}
      RUN_MODE: ${RUN_MODE}
      HAI_PROMETHEUS_ENABLED: ${HAI_PROMETHEUS_ENABLED}
      HAI_PROMETHEUS_TOKEN: ${HAI_PROMETHEUS_TOKEN}
      LLM_POLICY_JSON: ${LLM_POLICY_JSON}
      LLM_PROVIDERS_JSON: ${LLM_PROVIDERS_JSON}
      LLM_REQUIRE_RECENT_LIVE_PROBE: ${LLM_REQUIRE_RECENT_LIVE_PROBE}
      LLM_PROVIDER_PROBE_MAX_AGE_SECONDS: ${LLM_PROVIDER_PROBE_MAX_AGE_SECONDS}
      OLLAMA_BASE_URL: ${OLLAMA_BASE_URL}
      OLLAMA_MODEL_IDS: ${OLLAMA_MODEL_IDS:-}
      LLM_MODEL_MAINTENANCE_ENABLED: ${LLM_MODEL_MAINTENANCE_ENABLED:-true}
      LLM_MODEL_MAINTENANCE_INTERVAL_HOURS: ${LLM_MODEL_MAINTENANCE_INTERVAL_HOURS:-24}
      LLM_MODEL_MAINTENANCE_TIMEOUT_SECONDS: ${LLM_MODEL_MAINTENANCE_TIMEOUT_SECONDS:-900}
      LLM_MODEL_MAINTENANCE_FAILURE_RETRY_MINUTES: ${LLM_MODEL_MAINTENANCE_FAILURE_RETRY_MINUTES:-5}
      LLM_MODEL_MAINTENANCE_SCHEDULER_ENABLED: ${LLM_MODEL_MAINTENANCE_SCHEDULER_ENABLED:-true}
      LLM_MODEL_MAINTENANCE_SCHEDULER_INTERVAL_MINUTES: ${LLM_MODEL_MAINTENANCE_SCHEDULER_INTERVAL_MINUTES:-60}
      LM_STUDIO_BASE_URL: ${LM_STUDIO_BASE_URL}
      LLAMA_CPP_BASE_URL: ${LLAMA_CPP_BASE_URL}
      LLAMA_CPP_MODEL_ID: ${LLAMA_CPP_MODEL_ID}
      LOCALAI_BASE_URL: ${LOCALAI_BASE_URL}
      LOCALAI_MODEL_ID: ${LOCALAI_MODEL_ID}
      VLLM_BASE_URL: ${VLLM_BASE_URL}
      VLLM_MODEL_ID: ${VLLM_MODEL_ID}
      MISTRAL_RS_BASE_URL: ${MISTRAL_RS_BASE_URL}
      MISTRAL_RS_MODEL_ID: ${MISTRAL_RS_MODEL_ID}
      LITELLM_ENABLED: ${LITELLM_ENABLED}
      LITELLM_BASE_URL: ${LITELLM_BASE_URL}
      LITELLM_MODEL_ID: ${LITELLM_MODEL_ID}
      LITELLM_API_KEY: ${LITELLM_API_KEY:-}
      HAI_SEMANTIC_RETRIEVAL_ENABLED: ${HAI_SEMANTIC_RETRIEVAL_ENABLED}
      HAI_EMBEDDING_BASE_URL: ${HAI_EMBEDDING_BASE_URL}
      HAI_EMBEDDING_MODEL: ${HAI_EMBEDDING_MODEL}
      HAI_EMBEDDING_API_KEY: ${HAI_EMBEDDING_API_KEY:-}
      HAI_EMBEDDING_INPUT_LIMIT: ${HAI_EMBEDDING_INPUT_LIMIT}
      HAI_EMBEDDING_TIMEOUT_SECONDS: ${HAI_EMBEDDING_TIMEOUT_SECONDS}
      # Optional internal-only OR-Tools planner. It emits proposals only and
      # cannot apply workflow, calendar, filesystem, or external changes.
      HAI_PLANNING_OPTIMIZER_ENABLED: ${HAI_PLANNING_OPTIMIZER_ENABLED}
      HAI_PLANNING_OPTIMIZER_BASE_URL: ${HAI_PLANNING_OPTIMIZER_BASE_URL}
      HAI_PLANNING_OPTIMIZER_TIMEOUT_SECONDS: ${HAI_PLANNING_OPTIMIZER_TIMEOUT_SECONDS}
      HAI_TEMPORAL_ENABLED: ${HAI_TEMPORAL_ENABLED}
      HAI_TEMPORAL_ADDRESS: ${HAI_TEMPORAL_ADDRESS}
      HAI_TEMPORAL_NAMESPACE: ${HAI_TEMPORAL_NAMESPACE}
      HAI_TEMPORAL_TASK_QUEUE: ${HAI_TEMPORAL_TASK_QUEUE}
      HAI_PLAYWRIGHT_ENABLED: ${HAI_PLAYWRIGHT_ENABLED}
      HAI_PLAYWRIGHT_RUNNER_URL: ${HAI_PLAYWRIGHT_RUNNER_URL}
      HAI_PLAYWRIGHT_RUNNER_TOKEN: ${HAI_PLAYWRIGHT_RUNNER_TOKEN:-}
      HAI_PLAYWRIGHT_PROFILES: ${HAI_PLAYWRIGHT_PROFILES}
      HAI_SEARXNG_ENABLED: ${HAI_SEARXNG_ENABLED}
      HAI_SEARXNG_BASE_URL: ${HAI_SEARXNG_BASE_URL}
      HAI_RAGFLOW_ENABLED: ${HAI_RAGFLOW_ENABLED}
      HAI_RAGFLOW_BASE_URL: ${HAI_RAGFLOW_BASE_URL}
      HAI_RAGFLOW_API_KEY: ${HAI_RAGFLOW_API_KEY:-}
      HAI_RAGFLOW_DATASET_IDS: ${HAI_RAGFLOW_DATASET_IDS:-}
      HAI_ANYTHINGLLM_ENABLED: ${HAI_ANYTHINGLLM_ENABLED:-false}
      HAI_ANYTHINGLLM_BASE_URL: ${HAI_ANYTHINGLLM_BASE_URL:-http://host.docker.internal:3001}
      HAI_ANYTHINGLLM_API_KEY: ${HAI_ANYTHINGLLM_API_KEY:-}
      HAI_ANYTHINGLLM_WORKSPACE_SLUGS: ${HAI_ANYTHINGLLM_WORKSPACE_SLUGS:-}
      HAI_ANYTHINGLLM_LOCAL_EMBEDDINGS_CONFIRMED: ${HAI_ANYTHINGLLM_LOCAL_EMBEDDINGS_CONFIRMED:-false}
      HAI_SERENA_ENABLED: ${HAI_SERENA_ENABLED:-false}
      HAI_SERENA_BASE_URL: ${HAI_SERENA_BASE_URL:-http://host.docker.internal:9121/mcp}
      HAI_SERENA_PROJECT_ID: ${HAI_SERENA_PROJECT_ID:-}
      HAI_ODOO_ENABLED: ${HAI_ODOO_ENABLED:-false}
      HAI_ODOO_BASE_URL: ${HAI_ODOO_BASE_URL:-}
      HAI_ODOO_DATABASE: ${HAI_ODOO_DATABASE:-}
      HAI_ODOO_API_KEY: ${HAI_ODOO_API_KEY:-}
      HAI_ODOO_ALLOWED_MODELS: ${HAI_ODOO_ALLOWED_MODELS:-crm.lead,project.task}
      HAI_ODOO_SYNC_LIMIT: ${HAI_ODOO_SYNC_LIMIT:-20}
      HAI_SHARET_ENABLED: ${HAI_SHARET_ENABLED:-false}
      HAI_SHARET_BASE_URL: ${HAI_SHARET_BASE_URL:-}
      HAI_SHARET_CONNECTOR_TOKEN: ${HAI_SHARET_CONNECTOR_TOKEN:-}
      HAI_SHARET_SYNC_LIMIT: ${HAI_SHARET_SYNC_LIMIT:-1000}
      HAI_AIRBYTE_ENABLED: ${HAI_AIRBYTE_ENABLED:-false}
      HAI_AIRBYTE_BASE_URL: ${HAI_AIRBYTE_BASE_URL:-}
      HAI_AIRBYTE_API_KEY: ${HAI_AIRBYTE_API_KEY:-}
      HAI_AIRBYTE_WORKSPACE_IDS: ${HAI_AIRBYTE_WORKSPACE_IDS:-}
      HAI_LARO_ENABLED: ${HAI_LARO_ENABLED:-false}
      HAI_LARO_BASE_URL: ${HAI_LARO_BASE_URL:-}
      HAI_LARO_CONNECTOR_TOKEN: ${HAI_LARO_CONNECTOR_TOKEN:-}
      HAI_LARO_SYNC_LIMIT: ${HAI_LARO_SYNC_LIMIT:-50}
      HAI_CLOUDQUERY_SUMMARY_ENABLED: ${HAI_CLOUDQUERY_SUMMARY_ENABLED:-false}
      HAI_CLOUDQUERY_ALLOWED_ROOT: ${HAI_CLOUDQUERY_ALLOWED_ROOT:-/cloudquery-summary}
      HAI_CLOUDQUERY_SUMMARY_PATH: ${HAI_CLOUDQUERY_SUMMARY_PATH:-/cloudquery-summary/summary.jsonl}
      HAI_CLOUDQUERY_MAX_ENTRIES: ${HAI_CLOUDQUERY_MAX_ENTRIES:-50}
      HAI_PRESIDIO_ENABLED: ${HAI_PRESIDIO_ENABLED}
      HAI_PRESIDIO_BASE_URL: ${HAI_PRESIDIO_BASE_URL}
      HAI_PRESIDIO_LANGUAGE: ${HAI_PRESIDIO_LANGUAGE}
      HAI_PRESIDIO_ENTITIES: ${HAI_PRESIDIO_ENTITIES}
      HAI_EVIDENTLY_ENABLED: ${HAI_EVIDENTLY_ENABLED}
      HAI_EVIDENTLY_BASE_URL: ${HAI_EVIDENTLY_BASE_URL}
      HAI_EVIDENTLY_TIMEOUT_SECONDS: ${HAI_EVIDENTLY_TIMEOUT_SECONDS}
      HAI_GUARDRAILS_ENABLED: ${HAI_GUARDRAILS_ENABLED}
      HAI_GUARDRAILS_BASE_URL: ${HAI_GUARDRAILS_BASE_URL}
      HAI_GUARDRAILS_TIMEOUT_SECONDS: ${HAI_GUARDRAILS_TIMEOUT_SECONDS}
      HAI_PYDANTIC_AI_ENABLED: ${HAI_PYDANTIC_AI_ENABLED:-false}
      HAI_PYDANTIC_AI_BASE_URL: ${HAI_PYDANTIC_AI_BASE_URL:-http://pydantic-ai-runner:8080}
      HAI_PYDANTIC_AI_TIMEOUT_SECONDS: ${HAI_PYDANTIC_AI_TIMEOUT_SECONDS:-20}
      # Optional FastMCP read-only context bridge. The backend never receives
      # the MCP client token; it accepts only this separate service token and
      # returns bounded workflow summaries for one explicitly configured owner.
      HAI_FASTMCP_BRIDGE_ENABLED: ${HAI_FASTMCP_BRIDGE_ENABLED:-false}
      HAI_FASTMCP_BRIDGE_TOKEN: ${HAI_FASTMCP_BRIDGE_TOKEN:-}
      HAI_FASTMCP_BRIDGE_OWNER_ID: ${HAI_FASTMCP_BRIDGE_OWNER_ID:-}
      # Local A2A planning protocol subset. This token protects the direct
      # JSON-RPC endpoint; no browser session or generic backend API key is
      # accepted, and the endpoint can only return a planning draft.
      HAI_A2A_BRIDGE_ENABLED: ${HAI_A2A_BRIDGE_ENABLED:-false}
      HAI_A2A_BRIDGE_TOKEN: ${HAI_A2A_BRIDGE_TOKEN:-}
      HAI_A2A_BRIDGE_OWNER_ID: ${HAI_A2A_BRIDGE_OWNER_ID:-}
      HAI_A2A_BRIDGE_URL: ${HAI_A2A_BRIDGE_URL:-http://127.0.0.1/api/v1/a2a}
      HAI_LM_EVAL_ENABLED: ${HAI_LM_EVAL_ENABLED:-false}
      HAI_LM_EVAL_BASE_URL: ${HAI_LM_EVAL_BASE_URL:-http://lm-eval-runner:8080}
      HAI_LM_EVAL_TIMEOUT_SECONDS: ${HAI_LM_EVAL_TIMEOUT_SECONDS:-130}
      HAI_PROMPTFOO_ENABLED: ${HAI_PROMPTFOO_ENABLED:-false}
      HAI_PROMPTFOO_BASE_URL: ${HAI_PROMPTFOO_BASE_URL:-http://promptfoo-runner:8080}
      HAI_PROMPTFOO_TIMEOUT_SECONDS: ${HAI_PROMPTFOO_TIMEOUT_SECONDS:-130}
      HAI_DEEPTEAM_ENABLED: ${HAI_DEEPTEAM_ENABLED:-false}
      HAI_DEEPTEAM_BASE_URL: ${HAI_DEEPTEAM_BASE_URL:-http://deepteam-runner:8080}
      HAI_DEEPTEAM_TIMEOUT_SECONDS: ${HAI_DEEPTEAM_TIMEOUT_SECONDS:-180}
      HAI_DEEPEVAL_ENABLED: ${HAI_DEEPEVAL_ENABLED:-false}
      HAI_DEEPEVAL_BASE_URL: ${HAI_DEEPEVAL_BASE_URL:-http://deepeval-runner:8080}
      HAI_DEEPEVAL_TIMEOUT_SECONDS: ${HAI_DEEPEVAL_TIMEOUT_SECONDS:-180}
      HAI_GARAK_ENABLED: ${HAI_GARAK_ENABLED:-false}
      HAI_GARAK_BASE_URL: ${HAI_GARAK_BASE_URL:-http://garak-runner:8080}
      HAI_GARAK_TIMEOUT_SECONDS: ${HAI_GARAK_TIMEOUT_SECONDS:-150}
      HAI_WHISPER_CPP_ENABLED: ${HAI_WHISPER_CPP_ENABLED:-false}
      HAI_WHISPER_CPP_BASE_URL: ${HAI_WHISPER_CPP_BASE_URL:-http://whispercpp-runner:8080}
      HAI_WHISPER_CPP_TIMEOUT_SECONDS: ${HAI_WHISPER_CPP_TIMEOUT_SECONDS:-300}
      # Optional recovered runners. Every runner is disabled by default,
      # reachable only on a private Compose network, and retains no authority
      # outside its bounded backend adapter.
      HAI_GITLEAKS_ENABLED: ${HAI_GITLEAKS_ENABLED:-false}
      HAI_GITLEAKS_RUNNER_URL: ${HAI_GITLEAKS_RUNNER_URL:-http://gitleaks-runner:8080}
      HAI_GITLEAKS_RUNNER_TOKEN: ${HAI_GITLEAKS_RUNNER_TOKEN:-}
      HAI_GITLEAKS_WORKSPACES: ${HAI_GITLEAKS_WORKSPACES:-}
      HAI_GITLEAKS_TIMEOUT_SECONDS: ${HAI_GITLEAKS_TIMEOUT_SECONDS:-120}
      HAI_GOSEC_ENABLED: ${HAI_GOSEC_ENABLED:-false}
      HAI_GOSEC_RUNNER_URL: ${HAI_GOSEC_RUNNER_URL:-http://gosec-runner:8080}
      HAI_GOSEC_RUNNER_TOKEN: ${HAI_GOSEC_RUNNER_TOKEN:-}
      HAI_GOSEC_WORKSPACES: ${HAI_GOSEC_WORKSPACES:-}
      HAI_GOSEC_TIMEOUT_SECONDS: ${HAI_GOSEC_TIMEOUT_SECONDS:-120}
      HAI_GRYPE_ENABLED: ${HAI_GRYPE_ENABLED:-false}
      HAI_GRYPE_RUNNER_URL: ${HAI_GRYPE_RUNNER_URL:-http://grype-runner:8080}
      HAI_GRYPE_RUNNER_TOKEN: ${HAI_GRYPE_RUNNER_TOKEN:-}
      HAI_GRYPE_WORKSPACES: ${HAI_GRYPE_WORKSPACES:-}
      HAI_GRYPE_TIMEOUT_SECONDS: ${HAI_GRYPE_TIMEOUT_SECONDS:-120}
      HAI_SYFT_ENABLED: ${HAI_SYFT_ENABLED:-false}
      HAI_SYFT_RUNNER_URL: ${HAI_SYFT_RUNNER_URL:-http://syft-runner:8080}
      HAI_SYFT_RUNNER_TOKEN: ${HAI_SYFT_RUNNER_TOKEN:-}
      HAI_SYFT_WORKSPACES: ${HAI_SYFT_WORKSPACES:-}
      HAI_SYFT_TIMEOUT_SECONDS: ${HAI_SYFT_TIMEOUT_SECONDS:-120}
      HAI_TRIVY_ENABLED: ${HAI_TRIVY_ENABLED:-false}
      HAI_TRIVY_RUNNER_URL: ${HAI_TRIVY_RUNNER_URL:-http://trivy-runner:8080}
      HAI_TRIVY_RUNNER_TOKEN: ${HAI_TRIVY_RUNNER_TOKEN:-}
      HAI_TRIVY_WORKSPACES: ${HAI_TRIVY_WORKSPACES:-}
      HAI_TRIVY_TIMEOUT_SECONDS: ${HAI_TRIVY_TIMEOUT_SECONDS:-120}
      HAI_AGENT_FRAMEWORK_ENABLED: ${HAI_AGENT_FRAMEWORK_ENABLED:-false}
      HAI_AGENT_FRAMEWORK_BASE_URL: ${HAI_AGENT_FRAMEWORK_BASE_URL:-http://agent-framework-runner:8080}
      HAI_AGENT_FRAMEWORK_TIMEOUT_SECONDS: ${HAI_AGENT_FRAMEWORK_TIMEOUT_SECONDS:-120}
      HAI_CREWAI_ENABLED: ${HAI_CREWAI_ENABLED:-false}
      HAI_CREWAI_BASE_URL: ${HAI_CREWAI_BASE_URL:-http://crewai-runner:8080}
      HAI_CREWAI_TIMEOUT_SECONDS: ${HAI_CREWAI_TIMEOUT_SECONDS:-120}
      HAI_DOCLING_ENABLED: ${HAI_DOCLING_ENABLED:-false}
      HAI_DOCLING_RUNNER_URL: ${HAI_DOCLING_RUNNER_URL:-http://docling-runner:8080}
      HAI_DOCLING_RUNNER_TOKEN: ${HAI_DOCLING_RUNNER_TOKEN:-}
      HAI_DOCLING_TIMEOUT_SECONDS: ${HAI_DOCLING_TIMEOUT_SECONDS:-180}
      HAI_MINISWE_ENABLED: ${HAI_MINISWE_ENABLED:-false}
      HAI_MINISWE_RUNNER_URL: ${HAI_MINISWE_RUNNER_URL:-http://miniswe-runner:8080}
      HAI_MINISWE_RUNNER_TOKEN: ${HAI_MINISWE_RUNNER_TOKEN:-}
      HAI_MINISWE_WORKSPACES: ${HAI_MINISWE_WORKSPACES:-}
      HAI_MINISWE_TIMEOUT_SECONDS: ${HAI_MINISWE_TIMEOUT_SECONDS:-300}
      HAI_MLFLOW_ENABLED: ${HAI_MLFLOW_ENABLED:-false}
      HAI_MLFLOW_BASE_URL: ${HAI_MLFLOW_BASE_URL:-}
      HAI_MLFLOW_BEARER_TOKEN: ${HAI_MLFLOW_BEARER_TOKEN:-}
      HAI_MLFLOW_EXPERIMENT_IDS: ${HAI_MLFLOW_EXPERIMENT_IDS:-}
      HAI_MLFLOW_METRIC_KEYS: ${HAI_MLFLOW_METRIC_KEYS:-}
      HAI_MLFLOW_TIMEOUT_SECONDS: ${HAI_MLFLOW_TIMEOUT_SECONDS:-8}
      HAI_OPENLIT_ENABLED: ${HAI_OPENLIT_ENABLED:-false}
      HAI_OPENLIT_OTLP_ENDPOINT: ${HAI_OPENLIT_OTLP_ENDPOINT:-}
      HAI_OPENLIT_TIMEOUT_SECONDS: ${HAI_OPENLIT_TIMEOUT_SECONDS:-8}
      HAI_WASI_ENABLED: ${HAI_WASI_ENABLED}
      HAI_WASI_RUNNER_URL: ${HAI_WASI_RUNNER_URL}
      HAI_WASI_RUNNER_TOKEN: ${HAI_WASI_RUNNER_TOKEN:-}
      HAI_WASI_MODULES: ${HAI_WASI_MODULES}
      ODYSSEUS_BASE_URL: ${ODYSSEUS_BASE_URL}
      ODYSSEUS_API_TOKEN: ${ODYSSEUS_API_TOKEN}
      RUNTIME_LAB_ALLOWED_HOSTS: ${RUNTIME_LAB_ALLOWED_HOSTS}
      HERMES_BASE_URL: ${HERMES_BASE_URL}
      HERMES_HEALTH_PATH: ${HERMES_HEALTH_PATH}
      HERMES_API_SERVER_KEY: ${HERMES_API_SERVER_KEY:-}
      OPENCLAW_BASE_URL: ${OPENCLAW_BASE_URL}
      OPENCLAW_HEALTH_PATH: ${OPENCLAW_HEALTH_PATH}
      ODYSSEUS_HEALTH_PATH: ${ODYSSEUS_HEALTH_PATH}
      OPENHANDS_BASE_URL: ${OPENHANDS_BASE_URL}
      OPENHANDS_HEALTH_PATH: ${OPENHANDS_HEALTH_PATH}
      AGENT_RUNTIME_ALLOWED_HOSTS: ${AGENT_RUNTIME_ALLOWED_HOSTS}
      AGENT_RUNTIME_OUTPUT_LIMIT_BYTES: ${AGENT_RUNTIME_OUTPUT_LIMIT_BYTES}
      AGENT_RUNTIME_WORKSPACE_ROOT: ${AGENT_RUNTIME_WORKSPACE_ROOT}
      ODYSSEUS_AGENT_ENABLED: ${ODYSSEUS_AGENT_ENABLED}
      ODYSSEUS_AGENT_SESSION_ID: ${ODYSSEUS_AGENT_SESSION_ID}
      ODYSSEUS_AGENT_WORKSPACE: ${ODYSSEUS_AGENT_WORKSPACE}
      ODYSSEUS_AGENT_TIMEOUT_SECONDS: ${ODYSSEUS_AGENT_TIMEOUT_SECONDS}
      ODYSSEUS_CODEX_BRIDGE_ENABLED: ${ODYSSEUS_CODEX_BRIDGE_ENABLED}
      ODYSSEUS_CLAUDE_BRIDGE_ENABLED: ${ODYSSEUS_CLAUDE_BRIDGE_ENABLED}
      ODYSSEUS_TODOS_ENABLED: ${ODYSSEUS_TODOS_ENABLED}
      ODYSSEUS_NOTES_ENABLED: ${ODYSSEUS_NOTES_ENABLED}
      ODYSSEUS_TASKS_ENABLED: ${ODYSSEUS_TASKS_ENABLED}
      ODYSSEUS_EMAIL_ENABLED: ${ODYSSEUS_EMAIL_ENABLED}
      ODYSSEUS_CALENDAR_ENABLED: ${ODYSSEUS_CALENDAR_ENABLED}
      ODYSSEUS_CONTACTS_ENABLED: ${ODYSSEUS_CONTACTS_ENABLED}
      ODYSSEUS_DOCUMENTS_ENABLED: ${ODYSSEUS_DOCUMENTS_ENABLED}
      ODYSSEUS_MEMORY_SYNC_ENABLED: ${ODYSSEUS_MEMORY_SYNC_ENABLED}
      ODYSSEUS_RESEARCH_ENABLED: ${ODYSSEUS_RESEARCH_ENABLED}
      ODYSSEUS_SEARCH_ENABLED: ${ODYSSEUS_SEARCH_ENABLED}
      ODYSSEUS_MCP_ENABLED: ${ODYSSEUS_MCP_ENABLED}
      ODYSSEUS_COOKBOOK_ENABLED: ${ODYSSEUS_COOKBOOK_ENABLED}
      ODYSSEUS_LOCAL_MODEL_DISCOVERY_ENABLED: ${ODYSSEUS_LOCAL_MODEL_DISCOVERY_ENABLED}
      ODYSSEUS_SHELL_ENABLED: ${ODYSSEUS_SHELL_ENABLED}
      ODYSSEUS_BROWSER_ENABLED: ${ODYSSEUS_BROWSER_ENABLED}
      ODYSSEUS_VAULT_ENABLED: ${ODYSSEUS_VAULT_ENABLED}
      ODYSSEUS_GALLERY_ENABLED: ${ODYSSEUS_GALLERY_ENABLED}
      ODYSSEUS_TTS_ENABLED: ${ODYSSEUS_TTS_ENABLED}
      ODYSSEUS_STT_ENABLED: ${ODYSSEUS_STT_ENABLED}
      ODYSSEUS_COMPANION_ENABLED: ${ODYSSEUS_COMPANION_ENABLED}
      ODYSSEUS_WEBHOOKS_ENABLED: ${ODYSSEUS_WEBHOOKS_ENABLED}
      ODYSSEUS_AGENT_MIGRATION_ENABLED: ${ODYSSEUS_AGENT_MIGRATION_ENABLED}
      ODYSSEUS_CONTEXT_BUDGET_ENABLED: ${ODYSSEUS_CONTEXT_BUDGET_ENABLED}
      ODYSSEUS_AGENT_ALLOW_BASH: ${ODYSSEUS_AGENT_ALLOW_BASH}
      ODYSSEUS_AGENT_ALLOW_WEB_SEARCH: ${ODYSSEUS_AGENT_ALLOW_WEB_SEARCH}
      ODYSSEUS_AGENT_ALLOW_RESEARCH: ${ODYSSEUS_AGENT_ALLOW_RESEARCH}
      HERMES_AGENT_ENABLED: ${HERMES_AGENT_ENABLED}
      HERMES_EXECUTABLE: ${HERMES_EXECUTABLE}
      HERMES_HOME: ${HERMES_HOME}
      HERMES_PROFILE: ${HERMES_PROFILE}
      HERMES_IGNORE_USER_CONFIG: ${HERMES_IGNORE_USER_CONFIG}
      HERMES_WORKSPACE: ${HERMES_WORKSPACE}
      HERMES_MAX_TURNS: ${HERMES_MAX_TURNS}
      HERMES_TIMEOUT_SECONDS: ${HERMES_TIMEOUT_SECONDS}
      HERMES_TOOLSETS: ${HERMES_TOOLSETS}
      HERMES_SKILLS: ${HERMES_SKILLS}
      HERMES_GATEWAY_ENABLED: ${HERMES_GATEWAY_ENABLED}
      HERMES_CRON_ENABLED: ${HERMES_CRON_ENABLED}
      HERMES_MCP_ENABLED: ${HERMES_MCP_ENABLED}
      HERMES_MOA_ENABLED: ${HERMES_MOA_ENABLED}
      HERMES_SUBAGENTS_ENABLED: ${HERMES_SUBAGENTS_ENABLED}
      HERMES_MEMORY_SYNC_ENABLED: ${HERMES_MEMORY_SYNC_ENABLED}
      HERMES_ACP_ENABLED: ${HERMES_ACP_ENABLED}
      HERMES_TERMINAL_BACKENDS: ${HERMES_TERMINAL_BACKENDS}
      HERMES_ENV_ALLOWLIST: ${HERMES_ENV_ALLOWLIST}
      OPENCLAW_AGENT_ENABLED: ${OPENCLAW_AGENT_ENABLED}
      OPENCLAW_EXECUTABLE: ${OPENCLAW_EXECUTABLE}
      OPENCLAW_GATEWAY_URL: ${OPENCLAW_GATEWAY_URL}
      OPENCLAW_GATEWAY_TOKEN: ${OPENCLAW_GATEWAY_TOKEN}
      OPENCLAW_STATE_DIR: ${OPENCLAW_STATE_DIR}
      OPENCLAW_CONFIG_PATH: ${OPENCLAW_CONFIG_PATH}
      OPENCLAW_ECOSYSTEM_PATH: ${OPENCLAW_ECOSYSTEM_PATH}
      OPENCLAW_WORKSPACE: ${OPENCLAW_WORKSPACE}
      OPENCLAW_TIMEOUT_SECONDS: ${OPENCLAW_TIMEOUT_SECONDS}
      OPENCLAW_THINKING: ${OPENCLAW_THINKING}
      OPENCLAW_ENV_ALLOWLIST: ${OPENCLAW_ENV_ALLOWLIST}
      OPENCLAW_AGENT_CLI_ENABLED: ${OPENCLAW_AGENT_CLI_ENABLED}
      OPENCLAW_GATEWAY_ENABLED: ${OPENCLAW_GATEWAY_ENABLED}
      OPENCLAW_MESSAGES_ENABLED: ${OPENCLAW_MESSAGES_ENABLED}
      OPENCLAW_SKILLS_ENABLED: ${OPENCLAW_SKILLS_ENABLED}
      OPENCLAW_PLUGINS_ENABLED: ${OPENCLAW_PLUGINS_ENABLED}
      OPENCLAW_MCP_ENABLED: ${OPENCLAW_MCP_ENABLED}
      OPENCLAW_MEMORY_ENABLED: ${OPENCLAW_MEMORY_ENABLED}
      OPENCLAW_CRON_ENABLED: ${OPENCLAW_CRON_ENABLED}
      OPENCLAW_BROWSER_ENABLED: ${OPENCLAW_BROWSER_ENABLED}
      OPENCLAW_CANVAS_ENABLED: ${OPENCLAW_CANVAS_ENABLED}
      OPENCLAW_NODES_ENABLED: ${OPENCLAW_NODES_ENABLED}
      OPENCLAW_VOICE_ENABLED: ${OPENCLAW_VOICE_ENABLED}
      OPENCLAW_TALK_ENABLED: ${OPENCLAW_TALK_ENABLED}
      OPENCLAW_WEBCHAT_ENABLED: ${OPENCLAW_WEBCHAT_ENABLED}
      OPENCLAW_PAIRING_ENABLED: ${OPENCLAW_PAIRING_ENABLED}
      OPENCLAW_EXEC_APPROVALS_ENABLED: ${OPENCLAW_EXEC_APPROVALS_ENABLED}
      OPENCLAW_HOST_TOOLS_ENABLED: ${OPENCLAW_HOST_TOOLS_ENABLED}
      OPENCLAW_PUBLIC_POSTING_ENABLED: ${OPENCLAW_PUBLIC_POSTING_ENABLED}
      OPENCLAW_WEB_SEARCH_ENABLED: ${OPENCLAW_WEB_SEARCH_ENABLED}
      OPENCLAW_MULTI_AGENT_ENABLED: ${OPENCLAW_MULTI_AGENT_ENABLED}
      OPENCLAW_APP_SDK_ENABLED: ${OPENCLAW_APP_SDK_ENABLED}
      OPENCLAW_PLUGIN_SDK_ENABLED: ${OPENCLAW_PLUGIN_SDK_ENABLED}
      OPENCLAW_LOCAL_MODELS_ENABLED: ${OPENCLAW_LOCAL_MODELS_ENABLED}
      OPENCLAW_ALLOW_HIGH_RISK_EXECUTION: ${OPENCLAW_ALLOW_HIGH_RISK_EXECUTION}
      OPENCLAW_SANDBOX_REQUIRED: ${OPENCLAW_SANDBOX_REQUIRED}
      OPENCLAW_SANDBOX_MODE: ${OPENCLAW_SANDBOX_MODE}
      OPENCLAW_SANDBOX_DOCKER_ENABLED: ${OPENCLAW_SANDBOX_DOCKER_ENABLED}
      OPENCLAW_SANDBOX_SSH_ENABLED: ${OPENCLAW_SANDBOX_SSH_ENABLED}
      OPENCLAW_SANDBOX_OPENSHELL_ENABLED: ${OPENCLAW_SANDBOX_OPENSHELL_ENABLED}
      OPENCLAW_CHANNELS_ENABLED: ${OPENCLAW_CHANNELS_ENABLED}
      OPENCLAW_PROVIDERS_ENABLED: ${OPENCLAW_PROVIDERS_ENABLED}
      OPENCLAW_COMPANION_APPS: ${OPENCLAW_COMPANION_APPS}
      FREE_CLOUD_OPENAI_BASE_URL: ${FREE_CLOUD_OPENAI_BASE_URL}
      FREE_CLOUD_API_KEY: ${FREE_CLOUD_API_KEY}
      LLM_GENERATION_TIMEOUT_SECONDS: ${LLM_GENERATION_TIMEOUT_SECONDS}
      HAI_MCP_PREFLIGHT_ENABLED: ${HAI_MCP_PREFLIGHT_ENABLED}
      HAI_MCP_PREFLIGHT_SERVERS: ${HAI_MCP_PREFLIGHT_SERVERS}
      HAI_MCP_PREFLIGHT_TOKENS: ${HAI_MCP_PREFLIGHT_TOKENS:-}
      HAI_MCP_PREFLIGHT_ALLOW_REMOTE: ${HAI_MCP_PREFLIGHT_ALLOW_REMOTE:-false}
      HAI_MCP_PREFLIGHT_TIMEOUT_SECONDS: ${HAI_MCP_PREFLIGHT_TIMEOUT_SECONDS}
      HAI_CHATGPT_LOGS_MCP_ENABLED: ${HAI_CHATGPT_LOGS_MCP_ENABLED:-false}
      HAI_CHATGPT_LOGS_MCP_URL: ${HAI_CHATGPT_LOGS_MCP_URL:-http://host.docker.internal:8099/mcp}
      HAI_CHATGPT_LOGS_MCP_TIMEOUT_SECONDS: ${HAI_CHATGPT_LOGS_MCP_TIMEOUT_SECONDS:-8}
      HAI_CHATGPT_LOGS_MCP_TOKEN: ${HAI_CHATGPT_LOGS_MCP_TOKEN:-}
      HAI_CHATGPT_LOGS_MCP_ALLOW_REMOTE: ${HAI_CHATGPT_LOGS_MCP_ALLOW_REMOTE:-false}
      LLM_ALLOW_LINK_LOCAL_ENDPOINTS: ${LLM_ALLOW_LINK_LOCAL_ENDPOINTS}
      AUTOMATION_API_ALLOWED_HOSTS: ${AUTOMATION_API_ALLOWED_HOSTS}
      AUTOMATION_API_ALLOW_LINK_LOCAL: ${AUTOMATION_API_ALLOW_LINK_LOCAL}
      AUTOMATION_HEALTH_ALLOWED_HOSTS: ${AUTOMATION_HEALTH_ALLOWED_HOSTS}
      AUTOMATION_HEALTH_ALLOW_LINK_LOCAL: ${AUTOMATION_HEALTH_ALLOW_LINK_LOCAL}
      AUTOMATION_SCRIPT_EXECUTION_ENABLED: ${AUTOMATION_SCRIPT_EXECUTION_ENABLED}
      AUTOMATION_SCRIPT_DIR: ${AUTOMATION_SCRIPT_DIR}
      AUTOMATION_SCRIPT_TIMEOUT_SECONDS: ${AUTOMATION_SCRIPT_TIMEOUT_SECONDS}
      AUTOMATION_SCRIPT_OUTPUT_LIMIT_BYTES: ${AUTOMATION_SCRIPT_OUTPUT_LIMIT_BYTES:-4096}
      AUTOMATION_SCRIPT_ENV_ALLOWLIST: ${AUTOMATION_SCRIPT_ENV_ALLOWLIST}
      AUTOMATION_SCRIPT_SHA256_ALLOWLIST: ${AUTOMATION_SCRIPT_SHA256_ALLOWLIST}
      AUTOMATION_DOCKER_CONTROL_ENABLED: ${AUTOMATION_DOCKER_CONTROL_ENABLED}
      AUTOMATION_DOCKER_SOCKET: ${AUTOMATION_DOCKER_SOCKET}
      AUTOMATION_DOCKER_ALLOWED_CONTAINERS: ${AUTOMATION_DOCKER_ALLOWED_CONTAINERS}
      CONNECTED_SOURCE_LOCAL_ROOT: ${CONNECTED_SOURCE_LOCAL_ROOT}
      CONNECTED_SOURCE_FILE_LIMIT: ${CONNECTED_SOURCE_FILE_LIMIT}
      CONNECTED_SOURCE_MAX_BYTES: ${CONNECTED_SOURCE_MAX_BYTES}
      CONNECTED_SOURCE_HTTP_ALLOWED_HOSTS: ${CONNECTED_SOURCE_HTTP_ALLOWED_HOSTS}
      CONNECTED_SOURCE_HTTP_ALLOW_LINK_LOCAL: ${CONNECTED_SOURCE_HTTP_ALLOW_LINK_LOCAL}
      CONNECTED_SOURCE_HTTP_TIMEOUT_SECONDS: ${CONNECTED_SOURCE_HTTP_TIMEOUT_SECONDS}
      CONNECTED_SOURCE_HTTP_MAX_BYTES: ${CONNECTED_SOURCE_HTTP_MAX_BYTES}
      GITHUB_SOURCE_API_BASE_URL: ${GITHUB_SOURCE_API_BASE_URL}
      GITHUB_SOURCE_TOKEN: ${GITHUB_SOURCE_TOKEN}
      SOURCE_SCHEDULER_ENABLED: ${SOURCE_SCHEDULER_ENABLED}
      SOURCE_SCHEDULER_INTERVAL_SECONDS: ${SOURCE_SCHEDULER_INTERVAL_SECONDS}
      SOURCE_SCHEDULER_RUN_ON_STARTUP: ${SOURCE_SCHEDULER_RUN_ON_STARTUP}
      WORKFLOW_SCHEDULER_ENABLED: ${WORKFLOW_SCHEDULER_ENABLED}
      WORKFLOW_OPEN_LOOP_SCHEDULER_ENABLED: ${WORKFLOW_OPEN_LOOP_SCHEDULER_ENABLED}
      WORKFLOW_SCHEDULER_INTERVAL_SECONDS: ${WORKFLOW_SCHEDULER_INTERVAL_SECONDS}
      WORKFLOW_SCHEDULER_RUN_LIMIT: ${WORKFLOW_SCHEDULER_RUN_LIMIT}
      WORKFLOW_SCHEDULER_RUN_ON_STARTUP: ${WORKFLOW_SCHEDULER_RUN_ON_STARTUP}
      WORKFLOW_CLAIM_LEASE_SECONDS: ${WORKFLOW_CLAIM_LEASE_SECONDS}
      AMBIENT_SCHEDULER_ENABLED: ${AMBIENT_SCHEDULER_ENABLED}
      AMBIENT_EXECUTION_ENABLED: ${AMBIENT_EXECUTION_ENABLED}
      AMBIENT_RUN_ON_STARTUP: ${AMBIENT_RUN_ON_STARTUP}
      AMBIENT_SCAN_INTERVAL_SECONDS: ${AMBIENT_SCAN_INTERVAL_SECONDS}
      AMBIENT_MINIMUM_SCORE: ${AMBIENT_MINIMUM_SCORE}
      AMBIENT_MINIMUM_CONFIDENCE: ${AMBIENT_MINIMUM_CONFIDENCE}
      AMBIENT_OPPORTUNITY_LIMIT: ${AMBIENT_OPPORTUNITY_LIMIT}
      AMBIENT_EXECUTION_LIMIT: ${AMBIENT_EXECUTION_LIMIT}
      AMBIENT_DISMISS_COOLDOWN_HOURS: ${AMBIENT_DISMISS_COOLDOWN_HOURS}
      AMBIENT_SCAN_RETENTION: ${AMBIENT_SCAN_RETENTION}
      OUTCOME_MONITOR_SCHEDULER_ENABLED: ${OUTCOME_MONITOR_SCHEDULER_ENABLED:-true}
      OUTCOME_MONITOR_SWEEP_SECONDS: ${OUTCOME_MONITOR_SWEEP_SECONDS:-300}
      OUTCOME_MONITOR_POLL_SECONDS: ${OUTCOME_MONITOR_POLL_SECONDS:-15}
      OUTCOME_MONITOR_LEASE_SECONDS: ${OUTCOME_MONITOR_LEASE_SECONDS:-120}
      OUTCOME_MONITOR_SCOPE_LIMIT: ${OUTCOME_MONITOR_SCOPE_LIMIT:-50}
      OUTCOME_MONITOR_BATCH_LIMIT: ${OUTCOME_MONITOR_BATCH_LIMIT:-20}
      AUTONOMY_WORLD_STATE_TTL_SECONDS: ${AUTONOMY_WORLD_STATE_TTL_SECONDS}
      AUTONOMY_TELEMETRY_LIMIT: ${AUTONOMY_TELEMETRY_LIMIT}
      LLM_KV_CACHE_LOAD_STRATEGY: ${LLM_KV_CACHE_LOAD_STRATEGY}
      LLM_DUALPATH_INFRASTRUCTURE_VERIFIED: ${LLM_DUALPATH_INFRASTRUCTURE_VERIFIED}
    ports:
      - "127.0.0.1:${BACKEND_PORT}:${SERVER_PORT}"
    volumes:
      - ./images:/root/images
      - ./automation-scripts:/root/automation-scripts:ro
      - ./connected-sources:/root/connected-sources:ro
      - ./agent-workspaces:/root/agent-workspaces
      - ${CLOUDQUERY_SUMMARY_HOST_DIR:-./cloudquery-summary}:/cloudquery-summary:ro
    # /readyz, not a TCP probe: an open socket only proves the process is
    # listening. curl -f fails on the 503 that /readyz returns when a critical
    # dependency is unreachable, so `docker ps` stops reporting a backend with a
    # dead database as healthy.
    healthcheck:
      test: ["CMD-SHELL", "curl -fsS http://127.0.0.1:${SERVER_PORT}/readyz > /dev/null"]
      interval: 10s
      timeout: 5s
      # 3 strikes (~30s) before unhealthy: long enough to ride out a dependency
      # blip, short enough that a real outage is reflected quickly. start_period
      # still covers the slower first boot while migrations run.
      retries: 3
      start_period: 40s
    networks:
      - service-hub
      - automation-hub-network
      - evidently-internal
      - guardrails-internal
      - lm-eval-internal
      - promptfoo-internal
      - deepteam-internal
      - deepeval-internal
      - garak-internal
      - whispercpp-internal
      - pydantic-ai-internal
      - fastmcp-internal
      - security-runners-internal
      - agent-runners-internal
      - document-runners-internal
      - coding-runners-internal

  frontend:
    build:
      context: ./frontend
      dockerfile: Dockerfile
    container_name: 018-hai-frontend
    hostname: frontend
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_started
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - service-hub

  browser-verifier:
    build:
      context: ./services/browser-verifier
      dockerfile: Dockerfile
    profiles: ["verification"]
    restart: unless-stopped
    depends_on:
      frontend:
        condition: service_healthy
    environment:
      HAI_PLAYWRIGHT_RUNNER_TOKEN: ${HAI_PLAYWRIGHT_RUNNER_TOKEN:-}
      HAI_PLAYWRIGHT_ALLOWED_ORIGINS: ${HAI_PLAYWRIGHT_ALLOWED_ORIGINS}
    healthcheck:
      test:
        - CMD-SHELL
        - >-
          node -e 'fetch("http://127.0.0.1:8080/healthz").then(r => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))'
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 15s
    networks:
      - service-hub

  # These scanners operate only on operator-created disposable snapshots under
  # ./security-snapshots. They cannot inspect the repository, Docker socket,
  # connected sources, or host filesystem unless content is copied there.
  gitleaks-runner:
    build:
      context: ./services/gitleaks-runner
    profiles: ["security-scanning"]
    restart: unless-stopped
    environment:
      HAI_GITLEAKS_RUNNER_TOKEN: ${HAI_GITLEAKS_RUNNER_TOKEN:-}
      HAI_GITLEAKS_INPUT_ROOT: /inputs
      HAI_GITLEAKS_WORKSPACES: ${HAI_GITLEAKS_WORKSPACES:-}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=128m"]
    volumes:
      - ./security-snapshots:/inputs:ro
    mem_limit: 512m
    cpus: 1.0
    pids_limit: 128
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks: [security-runners-internal]

  gosec-runner:
    build:
      context: ./services/gosec-runner
    profiles: ["security-scanning"]
    restart: unless-stopped
    environment:
      HAI_GOSEC_RUNNER_TOKEN: ${HAI_GOSEC_RUNNER_TOKEN:-}
      HAI_GOSEC_INPUT_ROOT: /inputs
      HAI_GOSEC_WORKSPACES: ${HAI_GOSEC_WORKSPACES:-}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=256m"]
    volumes:
      - ./security-snapshots:/inputs:ro
    mem_limit: 768m
    cpus: 1.5
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python3", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks: [security-runners-internal]

  syft-runner:
    build:
      context: ./services/syft-runner
    profiles: ["security-scanning"]
    restart: unless-stopped
    environment:
      HAI_SYFT_RUNNER_TOKEN: ${HAI_SYFT_RUNNER_TOKEN:-}
      HAI_SYFT_INPUT_ROOT: /inputs
      HAI_SYFT_WORKSPACES: ${HAI_SYFT_WORKSPACES:-}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=256m"]
    volumes:
      - ./security-snapshots:/inputs:ro
    mem_limit: 768m
    cpus: 1.5
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks: [security-runners-internal]

  grype-runner:
    build:
      context: ./services/grype-runner
    profiles: ["security-scanning"]
    restart: unless-stopped
    environment:
      HAI_GRYPE_RUNNER_TOKEN: ${HAI_GRYPE_RUNNER_TOKEN:-}
      HAI_GRYPE_INPUT_ROOT: /inputs
      HAI_GRYPE_DB_ROOT: /grype-db
      HAI_GRYPE_WORKSPACES: ${HAI_GRYPE_WORKSPACES:-}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=256m"]
    volumes:
      - ./security-snapshots:/inputs:ro
      - ./grype-db:/grype-db:ro
    mem_limit: 1g
    cpus: 1.5
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks: [security-runners-internal]

  trivy-runner:
    build:
      context: ./services/trivy-runner
    profiles: ["security-scanning"]
    restart: unless-stopped
    environment:
      HAI_TRIVY_RUNNER_TOKEN: ${HAI_TRIVY_RUNNER_TOKEN:-}
      HAI_TRIVY_INPUT_ROOT: /inputs
      HAI_TRIVY_WORKSPACES: ${HAI_TRIVY_WORKSPACES:-}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=384m"]
    volumes:
      - ./security-snapshots:/inputs:ro
    mem_limit: 1g
    cpus: 1.5
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python3", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks: [security-runners-internal]

  # One private Ollama instance supports only disposable patch proposals. It
  # publishes no host port. Model tags are pulled by HAI's dedicated mini-SWE
  # maintenance gate before use; image upgrades remain an operator action.
  ollama-miniswe:
    image: ${HAI_RUNNER_OLLAMA_IMAGE:-ollama/ollama:0.11.10}
    profiles: ["patch-proposals"]
    restart: unless-stopped
    expose: ["11434"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=512m"]
    volumes:
      - runner-ollama-data:/root/.ollama
    mem_limit: 6g
    cpus: 4.0
    pids_limit: 512
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "ollama", "list"]
      interval: 15s
      timeout: 10s
      retries: 6
      start_period: 20s
    networks: [coding-runners-internal]

  agent-framework-runner:
    build:
      context: ./services/agent-framework-runner
    profiles: ["agent-framework-planning"]
    restart: unless-stopped
    environment:
      HAI_AGENT_FRAMEWORK_LOCAL_MODEL_BASE_URL: ${HAI_AGENT_FRAMEWORK_LOCAL_MODEL_BASE_URL:-http://host.docker.internal:11434/v1}
      HAI_AGENT_FRAMEWORK_LOCAL_MODEL_ID: ${HAI_AGENT_FRAMEWORK_LOCAL_MODEL_ID:-}
      HAI_AGENT_FRAMEWORK_LOCAL_MODEL_API_KEY: ${HAI_AGENT_FRAMEWORK_LOCAL_MODEL_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=256m"]
    mem_limit: 768m
    cpus: 1.0
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks: [agent-runners-internal]

  crewai-runner:
    build:
      context: ./services/crewai-runner
    profiles: ["crewai-planning"]
    restart: unless-stopped
    environment:
      HAI_CREWAI_LOCAL_MODEL_BASE_URL: ${HAI_CREWAI_LOCAL_MODEL_BASE_URL:-http://host.docker.internal:11434/v1}
      HAI_CREWAI_LOCAL_MODEL_ID: ${HAI_CREWAI_LOCAL_MODEL_ID:-}
      HAI_CREWAI_LOCAL_MODEL_API_KEY: ${HAI_CREWAI_LOCAL_MODEL_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=256m"]
    mem_limit: 768m
    cpus: 1.0
    pids_limit: 192
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks: [agent-runners-internal]

  docling-runner:
    build:
      context: ./services/docling-runner
    profiles: ["local-document-extraction"]
    restart: unless-stopped
    environment:
      HAI_DOCLING_RUNNER_TOKEN: ${HAI_DOCLING_RUNNER_TOKEN:-}
      HAI_DOCLING_INPUT_ROOT: /intake
      HAI_DOCLING_ARTIFACTS_ROOT: /models
      HAI_DOCLING_PDF_ENABLED: ${HAI_DOCLING_PDF_ENABLED:-false}
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,noexec,nosuid,size=1g"]
    volumes:
      - ./connected-sources:/intake:ro
      - ./docling-models:/models:ro
    mem_limit: 3g
    cpus: 2.0
    pids_limit: 256
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 40s
    networks: [document-runners-internal]

  miniswe-runner:
    build:
      context: ./services/miniswe-runner
    profiles: ["patch-proposals"]
    restart: unless-stopped
    depends_on:
      ollama-miniswe:
        condition: service_healthy
    environment:
      HAI_MINISWE_RUNNER_TOKEN: ${HAI_MINISWE_RUNNER_TOKEN:-}
      HAI_MINISWE_OLLAMA_BASE_URL: http://ollama-miniswe:11434
      HAI_MINISWE_MODEL_ID: ${HAI_MINISWE_MODEL_ID:-}
      HAI_MINISWE_INPUT_ROOT: /inputs
    expose: ["8080"]
    read_only: true
    tmpfs: ["/tmp:rw,nosuid,size=1g"]
    volumes:
      - ./mini-swe-workspaces:/inputs:ro
    mem_limit: 1g
    cpus: 1.5
    pids_limit: 256
    security_opt: ["no-new-privileges:true"]
    cap_drop: ["ALL"]
    healthcheck:
      test: ["CMD", "python", "-c", "from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks: [coding-runners-internal]

  nginx:
    image: nginx:alpine
    container_name: gateway
    hostname: gateway
    restart: unless-stopped
    environment:
      BACKEND_API_SHARED_KEY: ${BACKEND_API_SHARED_KEY}
    command:
      - /bin/sh
      - -c
      - envsubst '$$BACKEND_API_SHARED_KEY' < /etc/nginx/nginx.conf.template > /etc/nginx/nginx.conf && exec nginx -g 'daemon off;'
    depends_on:
      frontend:
        condition: service_healthy
      backend:
        condition: service_started
      idp:
        condition: service_started
    ports:
      - "${GATEWAY_HOST_BIND:-127.0.0.1}:${GATEWAY_HOST_PORT:-8088}:80"
    volumes:
      - ./nginx-config/nginx.conf.template:/etc/nginx/nginx.conf.template:ro
      - ./nginx-config/sites-enabled:/etc/nginx/sites-enabled:ro
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s
    networks:
      - service-hub

  # Explicit opt-in public HTTPS tunnel. Start it only through
  # scripts/start-ngrok.ps1, whose preflight rejects local-login bypass,
  # insecure cookies, placeholder secrets, and mismatched OAuth callbacks.
  # The inspector/API is reachable only inside this container.
  ngrok:
    image: ngrok/ngrok:alpine@sha256:85bea0e9c697bdad142b09cd9704420812cc44b5ae417ec4c7ac74d0a49d72f2
    profiles: ["cloud-tunnel"]
    container_name: 018-hai-ngrok
    restart: unless-stopped
    depends_on:
      nginx:
        condition: service_healthy
    environment:
      NGROK_AUTHTOKEN: ${NGROK_AUTHTOKEN:-}
      HAI_NGROK_URL: ${HAI_NGROK_URL:-}
      RUN_MODE: ${RUN_MODE:-production}
      LOCAL_LOGIN_BYPASS_ENABLED: ${LOCAL_LOGIN_BYPASS_ENABLED:-false}
      IDP_COOKIE_SECURE: ${IDP_COOKIE_SECURE:-false}
      GATEWAY_HOST_BIND: ${GATEWAY_HOST_BIND:-127.0.0.1}
    entrypoint: ["/bin/sh", "/etc/hai/start-ngrok.sh"]
    volumes:
      - ./deploy/ngrok/ngrok.yml:/etc/ngrok.yml:ro
      - ./deploy/ngrok/start-ngrok.sh:/etc/hai/start-ngrok.sh:ro
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=16m
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 128m
    cpus: 0.25
    pids_limit: 64
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O - http://127.0.0.1:4040/api/endpoints | grep -q '\"url\"'"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 15s
    networks:
      - service-hub

  nginxconfigmanager:
    build:
      context: ./nginx-config-manager
      dockerfile: Dockerfile
    container_name: 018-hai-nginx-config-manager
    restart: unless-stopped
    environment:
      BROKERS_ADDR: ${BROKERS_ADDR}
      KAFKA_BROKERS: ${KAFKA_BROKERS}
      KAFKA_TOPIC: ${KAFKA_TOPIC}
      CONFIG_DIR: ${CONFIG_DIR}
      NGINX_RELOAD_ENABLED: ${NGINX_RELOAD_ENABLED}
    depends_on:
      kafka:
        condition: service_healthy
      nginx:
        condition: service_started
    volumes:
      - ./nginx-config/sites-enabled:/app/sites-enabled
    networks:
      - service-hub

  ortools-solver:
    build:
      context: ./services/ortools-solver
    profiles: ["optimization"]
    restart: unless-stopped
    environment:
      MAX_SOLVE_SECONDS: ${HAI_PLANNING_OPTIMIZER_MAX_SOLVE_SECONDS:-2}
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - service-hub

  evidently-runner:
    build:
      context: ./services/evidently-runner
    profiles: ["evaluation"]
    restart: unless-stopped
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - evidently-internal

  guardrails-runner:
    build:
      context: ./services/guardrails-runner
    profiles: ["validation"]
    restart: unless-stopped
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - guardrails-internal

  pydantic-ai-runner:
    build:
      context: ./services/pydantic-ai-runner
    profiles: ["typed-planning"]
    restart: unless-stopped
    environment:
      HAI_PYDANTIC_AI_LOCAL_BASE_URL: ${HAI_PYDANTIC_AI_LOCAL_BASE_URL:-}
      HAI_PYDANTIC_AI_LOCAL_MODEL_ID: ${HAI_PYDANTIC_AI_LOCAL_MODEL_ID:-}
      HAI_PYDANTIC_AI_LOCAL_API_KEY: ${HAI_PYDANTIC_AI_LOCAL_API_KEY:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 512m
    cpus: 1.0
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 20s
    networks:
      - pydantic-ai-internal

  # An optional Streamable HTTP MCP endpoint for a reviewed local client. It
  # exposes exactly two read-only tools and is bound to the host loopback only.
  # It does not make HAI's normal API or any write/action endpoint available.
  fastmcp-bridge:
    build:
      context: ./services/fastmcp-bridge
    profiles: ["mcp-bridge"]
    restart: unless-stopped
    depends_on:
      backend:
        condition: service_healthy
    environment:
      HAI_FASTMCP_BRIDGE_ENABLED: ${HAI_FASTMCP_BRIDGE_ENABLED:-false}
      HAI_FASTMCP_BRIDGE_TOKEN: ${HAI_FASTMCP_BRIDGE_TOKEN:-}
      HAI_FASTMCP_CLIENT_TOKEN: ${HAI_FASTMCP_CLIENT_TOKEN:-}
      HAI_FASTMCP_BRIDGE_API_BASE_URL: ${HAI_FASTMCP_BRIDGE_API_BASE_URL:-http://backend:8080/api/v1/mcp-agent}
    ports:
      - "127.0.0.1:${HAI_FASTMCP_PORT:-8090}:8080"
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 256m
    cpus: 0.5
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - fastmcp-internal

  lm-eval-runner:
    build:
      context: ./services/lm-eval-runner
    profiles: ["model-evaluation"]
    restart: unless-stopped
    environment:
      HAI_LM_EVAL_MODEL_ID: ${HAI_LM_EVAL_MODEL_ID:-}
      HAI_LM_EVAL_MODEL_BASE_URL: ${HAI_LM_EVAL_MODEL_BASE_URL:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - lm-eval-internal

  promptfoo-runner:
    build:
      context: ./services/promptfoo-runner
    profiles: ["safety-evaluation"]
    restart: unless-stopped
    environment:
      HAI_PROMPTFOO_MODEL_ID: ${HAI_PROMPTFOO_MODEL_ID:-}
      HAI_PROMPTFOO_MODEL_BASE_URL: ${HAI_PROMPTFOO_MODEL_BASE_URL:-}
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 512m
    cpus: 1.0
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "node -e \"fetch('http://127.0.0.1:8080/healthz').then((r) => process.exit(r.ok ? 0 : 1)).catch(() => process.exit(1))\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - promptfoo-internal

  deepteam-runner:
    build:
      context: ./services/deepteam-runner
    profiles: ["deepteam-evaluation"]
    restart: unless-stopped
    environment:
      HAI_DEEPTEAM_MODEL_ID: ${HAI_DEEPTEAM_MODEL_ID:-}
      HAI_DEEPTEAM_MODEL_BASE_URL: ${HAI_DEEPTEAM_MODEL_BASE_URL:-}
      HAI_DEEPTEAM_MODEL_API_KEY: ${HAI_DEEPTEAM_MODEL_API_KEY:-}
      HTTP_PROXY: ""
      HTTPS_PROXY: ""
      ALL_PROXY: ""
      http_proxy: ""
      https_proxy: ""
      all_proxy: ""
      NO_PROXY: "*"
      no_proxy: "*"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m
    mem_limit: 768m
    cpus: 1.0
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - deepteam-internal

  deepeval-runner:
    build:
      context: ./services/deepeval-runner
    profiles: ["deepeval-evaluation"]
    restart: unless-stopped
    environment:
      HAI_DEEPEVAL_MODEL_ID: ${HAI_DEEPEVAL_MODEL_ID:-}
      HAI_DEEPEVAL_MODEL_BASE_URL: ${HAI_DEEPEVAL_MODEL_BASE_URL:-}
      HAI_DEEPEVAL_MODEL_API_KEY: ${HAI_DEEPEVAL_MODEL_API_KEY:-}
      DEEPEVAL_DISABLE_DOTENV: "1"
      DEEPEVAL_DISABLE_LEGACY_KEYFILE: "1"
      DEEPEVAL_TELEMETRY_OPT_OUT: "YES"
      HTTP_PROXY: ""
      HTTPS_PROXY: ""
      ALL_PROXY: ""
      http_proxy: ""
      https_proxy: ""
      all_proxy: ""
      NO_PROXY: "*"
      no_proxy: "*"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m,mode=1777
    mem_limit: 1024m
    cpus: 1.0
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 40s
    networks:
      - deepeval-internal

  garak-runner:
    build:
      context: ./services/garak-runner
    profiles: ["garak-evaluation"]
    restart: unless-stopped
    environment:
      HAI_GARAK_MODEL_ID: ${HAI_GARAK_MODEL_ID:-}
      HAI_GARAK_MODEL_BASE_URL: ${HAI_GARAK_MODEL_BASE_URL:-}
      HAI_GARAK_MODEL_API_KEY: ${HAI_GARAK_MODEL_API_KEY:-}
      HTTP_PROXY: ""
      HTTPS_PROXY: ""
      ALL_PROXY: ""
      http_proxy: ""
      https_proxy: ""
      all_proxy: ""
      NO_PROXY: "*"
      no_proxy: "*"
    extra_hosts:
      - "host.docker.internal:host-gateway"
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=256m
    mem_limit: 1024m
    cpus: 1.0
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 30s
    networks:
      - garak-internal

  whispercpp-runner:
    build:
      context: ./services/whispercpp-runner
    profiles: ["local-transcription"]
    restart: unless-stopped
    environment:
      WHISPER_CPP_MODEL_FILE: ${WHISPER_CPP_MODEL_FILE:-}
      WHISPER_CPP_LANGUAGE: ${WHISPER_CPP_LANGUAGE:-auto}
      WHISPER_CPP_FILE_LIMIT: ${WHISPER_CPP_FILE_LIMIT:-25}
      WHISPER_CPP_MAX_AUDIO_BYTES: ${WHISPER_CPP_MAX_AUDIO_BYTES:-67108864}
      WHISPER_CPP_TIMEOUT_SECONDS: ${WHISPER_CPP_TIMEOUT_SECONDS:-180}
    expose:
      - "8080"
    read_only: true
    tmpfs:
      - /tmp:rw,noexec,nosuid,size=768m
    volumes:
      - ./connected-sources:/intake:ro
      - ./whisper-models:/models:ro
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    mem_limit: 1g
    cpus: 2.0
    healthcheck:
      test: ["CMD", "python3", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8080/healthz', timeout=3).read()"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 15s
    networks:
      - whispercpp-internal

  wasi-runner:
    build:
      context: ./services/wasi-runner
    profiles: ["wasi"]
    restart: unless-stopped
    environment:
      HAI_WASI_RUNNER_TOKEN: ${HAI_WASI_RUNNER_TOKEN:-}
    volumes:
      - ./wasi-modules:/modules:ro
    read_only: true
    tmpfs:
      - /tmp
    mem_limit: 256m
    cpus: 0.5
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    healthcheck:
      test: ["CMD-SHELL", "python -c \"from urllib.request import urlopen; urlopen('http://127.0.0.1:8080/healthz', timeout=2).read()\""]
      interval: 15s
      timeout: 5s
      retries: 3
    networks:
      - service-hub

  # Optional, internal-only durable orchestration. It is intentionally split
  # from HAI's primary Postgres instance and is enabled only with
  # `--profile durability`. No Temporal port is exposed to the host.
  temporal-postgres:
    image: postgres:17-alpine
    profiles: ["durability"]
    restart: unless-stopped
    environment:
      POSTGRES_USER: temporal
      POSTGRES_PASSWORD: ${HAI_TEMPORAL_POSTGRES_PASSWORD:-temporal-local-only}
      POSTGRES_DB: temporal
    volumes:
      - temporal-postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U temporal -d temporal"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - service-hub

  temporal-schema:
    image: temporalio/admin-tools:1.31.2
    profiles: ["durability"]
    restart: "no"
    depends_on:
      temporal-postgres:
        condition: service_healthy
    environment:
      DB_PORT: 5432
      POSTGRES_SEEDS: temporal-postgres
      POSTGRES_USER: temporal
      POSTGRES_PWD: ${HAI_TEMPORAL_POSTGRES_PASSWORD:-temporal-local-only}
      SQL_PASSWORD: ${HAI_TEMPORAL_POSTGRES_PASSWORD:-temporal-local-only}
    volumes:
      - ./services/temporal/setup-postgres.sh:/scripts/setup-postgres.sh:ro
    entrypoint: ["/bin/sh", "/scripts/setup-postgres.sh"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - service-hub

  temporal:
    image: temporalio/server:1.31.2
    profiles: ["durability"]
    restart: unless-stopped
    depends_on:
      temporal-schema:
        condition: service_completed_successfully
    environment:
      DB: postgres12
      DB_PORT: 5432
      POSTGRES_USER: temporal
      POSTGRES_PWD: ${HAI_TEMPORAL_POSTGRES_PASSWORD:-temporal-local-only}
      POSTGRES_SEEDS: temporal-postgres
      BIND_ON_IP: 0.0.0.0
      DYNAMIC_CONFIG_FILE_PATH: config/dynamicconfig/development-sql.yaml
    expose:
      - "7233"
    volumes:
      - ./services/temporal/dynamicconfig:/etc/temporal/config/dynamicconfig:ro
    healthcheck:
      test: ["CMD", "nc", "-z", "127.0.0.1", "7233"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 30s
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - service-hub

  temporal-namespace:
    image: temporalio/admin-tools:1.31.2
    profiles: ["durability"]
    restart: "no"
    depends_on:
      temporal:
        condition: service_healthy
    environment:
      TEMPORAL_ADDRESS: temporal:7233
      DEFAULT_NAMESPACE: ${HAI_TEMPORAL_NAMESPACE:-default}
    volumes:
      - ./services/temporal/create-namespace.sh:/scripts/create-namespace.sh:ro
    entrypoint: ["/bin/sh", "/scripts/create-namespace.sh"]
    security_opt:
      - no-new-privileges:true
    cap_drop:
      - ALL
    networks:
      - service-hub

  postgres-idp:
    image: postgres:17-alpine
    container_name: 018-hai-postgres-idp
    hostname: postgres-idp
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${IDP_DB_NAME}
    ports:
      - "127.0.0.1:${IDP_DB_HOST_PORT:-5433}:5432"
    volumes:
      - postgres-idp-data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${IDP_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - idp-network

  postgres-automation:
    # Pinned upstream pgvector Postgres 17 image. Existing data stays in the
    # named volume; take a backup before changing a live database image.
    image: pgvector/pgvector:0.8.5-pg17-bookworm
    container_name: 018-hai-postgres-automation
    hostname: postgres-automation
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${DB_USER}
      POSTGRES_PASSWORD: ${DB_PASSWORD}
      POSTGRES_DB: ${AUTOMATION_DB_NAME}
    ports:
      - "127.0.0.1:${AUTOMATION_DB_HOST_PORT:-5434}:5432"
    volumes:
      - postgres-automation-data:/var/lib/postgresql/data
      - ./init.sql:/docker-entrypoint-initdb.d/init.sql:ro
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER} -d ${AUTOMATION_DB_NAME}"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - automation-hub-network

  redis:
    image: redis:7-alpine
    container_name: 018-hai-redis
    hostname: redis
    restart: unless-stopped
    command: ["redis-server", "--port", "${REDIS_PORT}"]
    ports:
      - "127.0.0.1:${REDIS_HOST_PORT:-6380}:${REDIS_PORT}"
    healthcheck:
      test: ["CMD", "redis-cli", "-p", "${REDIS_PORT}", "ping"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      # Also on automation-hub-network so the backend can reach it: Redis was
      # previously idp-network only, unreachable from the backend, which is why
      # nothing but the IDP ever used it.
      - idp-network
      - automation-hub-network

  zookeeper:
    image: confluentinc/cp-zookeeper:7.6.1
    container_name: 018-hai-zookeeper
    restart: unless-stopped
    environment:
      ZOOKEEPER_CLIENT_PORT: ${ZOOKEEPER_PORT}
      ZOOKEEPER_TICK_TIME: 2000
    healthcheck:
      test: ["CMD-SHELL", "nc -z 127.0.0.1 ${ZOOKEEPER_PORT}"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - kafka-network

  kafka:
    image: confluentinc/cp-kafka:7.6.1
    container_name: 018-hai-kafka
    hostname: kafka
    restart: unless-stopped
    depends_on:
      zookeeper:
        condition: service_healthy
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:${ZOOKEEPER_PORT}
      KAFKA_LISTENERS: INTERNAL://:9092
      KAFKA_ADVERTISED_LISTENERS: INTERNAL://kafka:9092
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: INTERNAL:PLAINTEXT
      KAFKA_INTER_BROKER_LISTENER_NAME: INTERNAL
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
      KAFKA_TRANSACTION_STATE_LOG_MIN_ISR: 1
      KAFKA_TRANSACTION_STATE_LOG_REPLICATION_FACTOR: 1
      KAFKA_AUTO_CREATE_TOPICS_ENABLE: "true"
    healthcheck:
      test: ["CMD-SHELL", "kafka-broker-api-versions --bootstrap-server 127.0.0.1:9092 >/dev/null 2>&1"]
      interval: 10s
      timeout: 10s
      retries: 18
      start_period: 30s
    networks:
      - kafka-network
      - service-hub

  generic-auto:
    build:
      context: ./generic-auto
      dockerfile: dockerfile
    container_name: 018-hai-generic-auto
    restart: unless-stopped
    healthcheck:
      test: ["CMD-SHELL", "wget -q --spider http://127.0.0.1/ || exit 1"]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 10s
    networks:
      - service-hub

networks:
  idp-network:
    name: 018-hai-idp-network
    driver: bridge
  kafka-network:
    name: 018-hai-kafka-network
    driver: bridge
  automation-hub-network:
    name: 018-hai-automation-hub-network
    driver: bridge
  service-hub:
    name: 018-hai-service-hub
    driver: bridge
  evidently-internal:
    name: 018-hai-evidently-internal
    driver: bridge
    internal: true
  guardrails-internal:
    name: 018-hai-guardrails-internal
    driver: bridge
    internal: true
  lm-eval-internal:
    name: 018-hai-lm-eval-internal
    driver: bridge
    internal: true
  promptfoo-internal:
    name: 018-hai-promptfoo-internal
    driver: bridge
    internal: true
  deepteam-internal:
    name: 018-hai-deepteam-internal
    driver: bridge
    internal: true
  deepeval-internal:
    name: 018-hai-deepeval-internal
    driver: bridge
    internal: true
  garak-internal:
    name: 018-hai-garak-internal
    driver: bridge
    internal: true
  whispercpp-internal:
    name: 018-hai-whispercpp-internal
    driver: bridge
    internal: true
  pydantic-ai-internal:
    name: 018-hai-pydantic-ai-internal
    driver: bridge
    internal: true
  fastmcp-internal:
    name: 018-hai-fastmcp-internal
    driver: bridge
    internal: true
  security-runners-internal:
    name: 018-hai-security-runners-internal
    driver: bridge
    internal: true
  agent-runners-internal:
    name: 018-hai-agent-runners-internal
    driver: bridge
    internal: true
  document-runners-internal:
    name: 018-hai-document-runners-internal
    driver: bridge
    internal: true
  coding-runners-internal:
    name: 018-hai-coding-runners-internal
    driver: bridge
    internal: true

volumes:
  postgres-idp-data:
    name: 018-hai-postgres-idp-data
  postgres-automation-data:
    name: 018-hai-postgres-automation-data
  temporal-postgres-data:
    name: 018-hai-temporal-postgres-data
  runner-ollama-data:
    name: 018-hai-runner-ollama-data
